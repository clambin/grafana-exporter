kind: ConfigMap
apiVersion: v1
metadata:
  name: grafana-dashboards-bar
  namespace: default
  labels:
    grafana_dashboard: ""
  annotations:
    grafana_folder: bar
data:
  foo.json: |
    {
      "folder": "bar",
      "title": "foo"
    }

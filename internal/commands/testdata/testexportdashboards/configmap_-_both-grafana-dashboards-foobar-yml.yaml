kind: ConfigMap
apiVersion: v1
metadata:
  name: grafana-dashboards-foobar
  namespace: default
  labels:
    grafana_dashboard: ""
  annotations:
    grafana_folder: foobar
data:
  snafu.json: |
    {
      "folder": "foobar",
      "title": "snafu"
    }
